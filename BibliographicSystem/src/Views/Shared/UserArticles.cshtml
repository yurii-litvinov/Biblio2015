@model List<BibliographicSystem.Models.Article>

<table class="table">
    <thead>
        <tr>
            <th>Название</th>
            <th>Описание</th>
        </tr>
    </thead>
    @foreach (var g in Model)
    {
        <tbody>
            <tr>
                <td>@g.Title</td>
                <td><a href="/Home/Details/@g.ArticleId">Подробнее..</a></td>
            </tr>
        </tbody>
    }
</table>